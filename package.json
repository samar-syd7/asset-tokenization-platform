{
  "name": "asset-registry",
  "version": "1.0.0",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "dev": "yarn workspace @asset-registry/nextjs dev",
    "build": "yarn workspace @asset-registry/nextjs build",
    "start": "yarn workspace @asset-registry/nextjs dev",
    "chain": "yarn workspace @asset-registry/hardhat node",
    "deploy": "yarn workspace @asset-registry/hardhat deploy",
    "compile": "yarn workspace @asset-registry/hardhat compile",
    "test": "yarn workspace @asset-registry/hardhat test",
    "lint": "yarn workspace @asset-registry/nextjs lint",
    "format": "yarn workspace @asset-registry/nextjs format"
  },
  "devDependencies": {
    "husky": "~9.1.6",
    "lint-staged": "~13.2.2"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.6.1"
  },
  "packageManager": "yarn@3.2.3",
  "engines": {
    "node": ">=20"
  }
}
